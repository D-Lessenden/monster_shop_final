<%#binding.pry %>
<% current_user.merchant.discounts.each do |discount| %>
<p><%= link_to "#{discount.name}", "#{current_user.merchant_id}/discounts/#{discount.id}/show" %></p>
<p><%=  %></p>
<% end %>
